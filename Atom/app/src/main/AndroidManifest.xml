<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="network.atom.atom">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".tabs_activity"
            android:theme="@style/MyMaterialTheme.Base">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".splash_activity" />
        <activity android:name=".signup_activity" />
        <activity
            android:name=".login_activity"
            android:theme="@style/MyMaterialTheme.Base" />
        <activity android:name=".posts_activity" />
        <activity android:name=".chats_activity" />
        <activity android:name=".friends_activity" />
        <activity android:name=".notifications_activity" />
        <activity android:name=".notes_activity"></activity>
    </application>

</manifest>